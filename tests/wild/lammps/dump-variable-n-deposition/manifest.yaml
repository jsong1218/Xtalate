case: dump-variable-n-deposition
format_id: lammps_dump
source_file: dump.lammpstrj
sha256: "e6dca0c61307788f3d04affb107ac20d5d1bad211b910798da344d634744cd3c"
origin:
  kind: synthetic
  source: "Authored-realistic LAMMPS dump (M49-S1): a three-snapshot deposition trajectory (3 -> 4 -> 4 atoms) in declared metal units — the grand-canonical/deposition shape, now a first-class read (schema 2.0.0, M72/M73) — generalizing the M46 dump block spelling."
  license: "Apache-2.0"
expectation:
  issue_codes: []
  frame_count: 3
  roundtrip: checked
notes: >-
  The genuine variable-N wild case (M49-S1 dimension 8; the v2.0 variable-N evidence file the
  release notes cite): the atom count varies across frames (a deposition grows the cell
  population 3 -> 4). Schema 2.0.0 (M72) lifted the constant-N invariant and M73 retired the
  reader refusal, so the file now parses to three frames, each carrying its own atom count (P3
  — a frame's N is the value it has, measured, never padded or truncated). The suite pins the
  per-frame counts [3, 4, 4] (test_variable_n_reads_through_measures_per_frame_counts), so the
  evidence stays real. roundtrip: checked — a dump writes one 'ITEM: NUMBER OF ATOMS' per
  snapshot, so the export re-writes every frame at its own N and the re-parse reproduces the
  object (the reported lammps_dump:type gain excepted, the M47 identity discipline). Authoring
  follows D167: the block spelling generalizes the committed M46 goldens; the counts are the
  file's own.
